User-agent: *
Allow: /

# Per-prospect scoring-map pages under /maps/ are deliberately NOT disallowed
# here. Each one carries <meta name="robots" content="noindex,nofollow">, and a
# crawler has to be able to fetch the page to see that instruction. Blocking
# the directory in robots.txt would prevent the noindex from ever being read.
